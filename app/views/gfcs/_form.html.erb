<%= form_with(model: gfc) do |form| %>
  <% if gfc.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(gfc.errors.count, "error") %> prohibited this gfc from being saved:</h2>

      <ul>
        <% gfc.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
